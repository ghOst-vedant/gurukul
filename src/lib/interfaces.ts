export interface decodedToken {}

export interface courseInterface {
  content: string[];
  curriculum: string[];
  type: string;
  students: string[];
}

export interface purchaseInfo {
  studentId: string;
  courseId: string;
}
