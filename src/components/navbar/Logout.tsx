import React from "react";

const Logout = () => {
  return (
    <div>
      <button
        onClick={() => {
        }}
      >
        logout
      </button>
    </div>
  );
};

export default Logout;
