import React from "react";

const Logout = () => {
  return (
    <div>
      <button
        onClick={() => {
          console.log("Hello");
        }}
      >
        logout
      </button>
    </div>
  );
};

export default Logout;
